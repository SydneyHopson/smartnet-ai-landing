import Link from "next/link";import{notFound}from"next/navigation";import{ArrowLeft}from"lucide-react";import{sanityWriteClient}from"@/lib/sanityWriteClient";import{OwnerQuoteClient}from"./OwnerQuoteClient";
type Lead={_id:string;contactName?:string|null;contactEmail?:string|null;contactPhone?:string|null;jobLocationNote?:string|null;projectType?:string|null;requestedServices?:string|null;estimateRoughRange?:string|null;estimateTotal?:number|null;focus?:string[]|null;walkthroughSiteNotes?:string|null;walkthroughEquipmentNotes?:string|null;walkthroughCableNotes?:string|null;walkthroughPowerNetworkNotes?:string|null};type Quote={status?:string|null;discount?:number|null;taxRate?:number|null;notes?:string|null;lineItems?:Array<{id?:string;description?:string;category?:string;quantity?:number;unitPrice?:number;unitCost?:number}>|null};export default async function QuotePage({params}:{params:Promise<{id:string}>}){const{id}=await params;const[lead,quote]=await Promise.all([sanityWriteClient.fetch<Lead|null>(`*[_type=="walkthroughBooking"&&_id==$id][0]{_id,contactName,contactEmail,contactPhone,jobLocationNote,projectType,requestedServices,estimateRoughRange,estimateTotal,focus,walkthroughSiteNotes,walkthroughEquipmentNotes,walkthroughCableNotes,walkthroughPowerNetworkNotes}`,{id}),sanityWriteClient.fetch<Quote|null>(`*[_type=="smartnetQuote"&&leadId==$id][0]{status,discount,taxRate,notes,lineItems}`,{id})]);if(!lead)notFound();return <main className="min-h-screen bg-[#010713] text-white"><div className="mx-auto max-w-[1450px] px-4 py-6 sm:px-6 lg:px-8"><Link href={`/owner/leads/${id}`} className="inline-flex h-10 items-center gap-2 rounded-xl border border-blue-400/20 bg-blue-500/[.06] px-3 text-[10px] font-semibold text-blue-200"><ArrowLeft className="h-4 w-4"/>Back to Lead</Link><div className="mt-5 rounded-2xl border border-blue-400/15 bg-[#041022] p-5 sm:p-6"><p className="text-[9px] uppercase tracking-[.2em] text-cyan-400">SmartNET Sales</p><div className="mt-2 flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-semibold sm:text-3xl">Quote · {lead.contactName||"Customer"}</h1><p className="mt-1 text-[10px] text-slate-500">{lead.jobLocationNote||lead.projectType||"SmartNET project"}</p></div><p className="text-sm font-semibold text-emerald-200">Estimator {lead.estimateRoughRange||lead.estimateTotal?`$${lead.estimateTotal?.toLocaleString()}`:"range pending"}</p></div></div><OwnerQuoteClient lead={lead} savedQuote={quote}/></div></main>}
